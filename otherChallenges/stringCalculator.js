var str = "14 + 4";

// function stringCalculatorEasy(str){

//     var number = eval(str);
//     console.log(number);
// }

function stringCalculator(str) {
    var length = str.length;


    var firstNum = 0

    for (var i = 0; str[i] != " "; i++) {
        // if (str[i] == " ") {
        //     firstNum = str.slice(0, i);

        //     var stop = i;

        // }
        console.log("hi");
        
    }

}
stringCalculator(str);




// function that takes a math problem in a string and evaluates it
