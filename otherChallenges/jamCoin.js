// google code jam challenge problem

// generate coin with a string of 0 and 1 values.
// must start and end with a one
// first nontrivial to must not be prime

var coin = 1001;

for (var i = 2; i < 11; i++){
    console.log(parseInt(coin, i));
    if (parseInt(coin, i) % 2 !=0){
        
    }
    else 
}

